class PokeCard {
    constructor(uuid, name,  photoURL) {
        this.id = uuid,
        this.name = name,
        this.photoURL = photoURL,
        this.clicked = false
    }
}

export default PokeCard